<p id="notice"><%= notice %></p>

<p>
  <strong>Description:</strong>
  <%= @install.description %>
</p>

<p>
  <strong>Area:</strong>
  <%= @install.area %>
</p>

<%= link_to 'Edit', edit_install_path(@install) %> |
<%= link_to 'Back', installs_path %>
